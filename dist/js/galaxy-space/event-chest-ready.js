define(["jquery","ajax","eventStatusDo"],($,ajax,eventStatusDo)=>(chest,targets)=>{ajax("PUT",`http://localhost:8080/chest/status/${chest.id}`,{status:"READY"}).then(eventStatusDo.ready.bind(eventStatusDo.ready,chest,targets))});